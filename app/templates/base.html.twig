<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    {% endblock %}
    <style>

        body {
            font-size: 18px;
        }

        .vertical-menu-contener {
            position: fixed;
            box-sizing: border-box;
            height: 100vh;
            max-width: 280px;
        }

        .menu-shadow {
            -webkit-box-shadow: 3px 3px 6px 0px rgba(102,102,102,1);
            -moz-box-shadow: 3px 3px 6px 0px rgba(102,102,102,1);
            box-shadow: 3px 3px 6px 0px rgba(102,102,102,1);
        }

        .vertical-menu {
            display: flex;
            flex-direction: column;
            height: 60%;
            align-items: flex-start;
            justify-content: space-between;
            margin-top: 200px;
            padding-left: 50px;
        }

        .fz-20px {
            font-size: 22px;
        }

        .pd-left-50px {
            padding-left: 50px;
        }

        .pd-right-100px {
            padding-right: 120px;
        }

        .content {
            margin-top: 160px;
        }

        .nav-bar-top {
            height: 80px;
        }
        .last-sublist-elem {
            margin-bottom: 100px;
        }

        .flex-column {
            display: flex;
            flex-direction: column;
            align-items: center;
        }


        .header {
            font-size: 4.2rem;
            margin-bottom: 100px;
        }

        .add-artcle-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 20px;
        }

        .articles-container {
            box-sizing: border-box;
            display: flex;
            flex-direction: row;
            align-items: flex-start;
            justify-content: space-between;
            flex-wrap: wrap;
            width: 100%;
            min-height: 50vh;
            padding: 60px 0 60px;
        }

        .categories-container,
        .authors-container {
            width: 100%;
            min-height: 50vh;
            padding: 60px 0 60px;
            margin: 0 auto;
        }

        .categories-container td,
        .authors-container td {
            padding: 20px;
        }

        .user-td,
        .category-td {
            font-size: 22px;
        }

        .user-td {
            margin-left: 10px;
        }

        .articles-item {
            flex-basis: 440px;
            margin-bottom: 100px;
            position: relative;
            background-color: #ffffff;
            padding: 10px;
        }

        .articles-item::after {
            content: '';
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background-color: #000000;
            position: absolute;
            -webkit-box-shadow: 0px 6px 10px 0px rgba(136,136,136,1);
            -moz-box-shadow: 0px 6px 10px 0px rgba(136,136,136,1);
            box-shadow: 0px 6px 10px 0px rgba(136,136,136,1);
            z-index: -1;
            opacity: 0;
            transition: opacity .2s ease-in-out;
            will-change: opacity;
        }

        .articles-item:hover::after {
            opacity: 1;
        }
        
        .active-link {
            color: rgb(52, 58, 64);
        }

        .articles-image {
            display: block;
            margin: 0 auto;
        }

        .articles-title {

        }

        .articles-author {
            display: flex;
            color: #111111;
            align-items: center;
            text-decoration: none;
        }

        .articles-author-name {
            display: flex;
            flex-direction: column;
        }

        .articles-author-name span {
            display: block;
            padding-left: 10px;
            font-weight: bold;
            color: #777;
            transition: color .2s ease-in-out;
            will-change: color;

        }

        .articles-author:hover .articles-author-name span {
            color: #111;
        }

        .articles-options {
            display: flex;
            width: 30%;
            position: absolute;
            bottom: 20px;
            right: 10px;
            justify-content: space-between;
        }

        .articles-image,
        .articles-title,
        .articles-author,
        .articles-category,
        .articles-date,
        .articles-options {
            padding-bottom: 10px;
        }


        .article-link {
            text-decoration: none;
            color: #111111;
        }

        .sort-bar {
            font-weight: bold;
            padding-top: 30px;
            text-align: left;
            display: flex;
            width: 30%;
            justify-content: space-between;
            margin: 0 auto;
        }

        .sort-item {

        }

        .ta-center {
            text-align: center;
        }

        .ta-left {
            text-align: left;
        }

        .ta-right {
            text-align: right;
        }

        .sub-header {
            margin-top: 50px;
        }

        .tags-container {
            margin: 50px 0;
            display: flex;
            min-height: 20vh;
            align-items: center;
            align-content: space-between;
            justify-content: flex-start;
            flex-wrap: wrap;
        }

        .tag {
            display: block;
            margin: 10px;
            min-width: 10%;
        }

        .tag .tag {
            font-size: 1rem;
            /*min-width: 120px;*/
            padding: 15px;
        }

        .article-header {
            position: relative;
        }

        .title-background {
            position: relative;
        }

        .edit-photo {
            position: absolute;
            right: 0;
            top: 0;
            color: #eeeeee;
            padding: 5px;
            background-color: rgba(0,0,0,.6);
        }

        .edit-photo:hover {
            color: #eeeeee;
        }

        .article-photo {
            width: 100%;
        }

        .article-title {
            position: absolute;
            width: 100%;
            text-align: center;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 5rem;
            padding: 20px 0 50px;
            color: #ffffff;
            background-color: rgba(0,0,0,.6);
        }

        .managed-article {
            display: flex;
            width: 100%;
            margin: 5px 0 20px;
            justify-content: space-between;
        }

        .article-managed-item {

        }

        .article-info {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .article-category,
        .article-published-date {
            display: flex;
            flex-direction: column;
        }

        .article-date-title,
        .article-category-title {
            font-weight: bolder;
        }

        .article-author-name {
            font-size: 1.4rem;
            position: relative;
            top: 5px;
            margin-left: 5px;
        }
        
        .article-date-item {
            color: #555555;
        }


        .article-content {
            width: 80%;
            margin: 0 auto;
            font-size: 20px;
            line-height: 2rem;
        }

        .article-content p {
            margin: 20px 0 60px;
        }

        .article-content img {
            display: block;
            width: 100%;
            margin: 20 auto 20px;
        }

        .article-content ul {
            margin: 20px 0 60px;
        }


        .comment {
            display: flex;
            width: 60%;
            flex-wrap: wrap;
            flex-direction: row;
            align-items: center;
            justify-content: flex-start;
            min-width: 200px;
            margin: 60px 30px 80px;
        }

        .comment-info {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            justify-content: space-between;
            margin-left: 10px;
        }

        .comment-content {
            width: 100%;
            margin: 15px 0 0 5px;
        }

        .comment-author {
            font-size: 1.2rem;
            font-weight: bolder;
        }

        .comment-date {
            color: #555;
            font-size: 1rem;
        }

        .user-section {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            margin: 0 0 60px 0;
        }

        .manage-avatar {
            position: relative;
            left: 10px;
        }

        .user-bio {
            width: 60%;
            margin: 0 60px 0 20px;
            font-weight: bolder;
            position: relative;
            top: -15px;
        }

        .user-name {
            width: 100%;
            position: relative;
            margin-top: 5px;
            left: 30px;
        }

        .manage-blog {
            display: flex;
            flex-direction: column;
            min-height: 100px;
            justify-content: space-between;
        }
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="fixed-top navbar navbar-dark nav-bar-top bg-dark text-white row menu-shadow ">
        <div class="pd-left-50px col-auto">
            <a class="nav-link text-light" href="{{ url('article_index') }}">
                Strona Główna
            </a>
        </div>
        <div class="col-5">
            <form method="get" action="{{ url('search_view') }}">
                <div id="search" class="row justify-content-center">
                    <input id="search_input" type="text" name="search" class="form-control col-9">
                    <input type="submit" value="{{ 'label.search'|trans }}" class="btn btn-primary col-2">
                </div>
            </form>
        </div>
        <div class="pd-right-100px col-auto">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <div class="d-inline-block btn-group" role="group" aria-label="Basic example">
                <a class="btn btn-outline-light" href="{{ url('security_logout') }}">wyloguj się</a>
                <a title="{{ app.user.firstName }}" href="{{ url('author_view', {id: app.user.id, firstName: app.user.firstName} ) }}">
                    {% if app.user.avatar.file.filename is defined %}
                        <img width="50px" class="nav-profile-img rounded-circle" src="{{ asset('uploads/photos/'~ app.user.avatar.file.filename) }}">
                        {% else %}
                            <img width="50px" class="nav-profile-img rounded-circle" src="{{ asset('uploads/photos/user_placeholder_image.jpg') }}">
                        {% endif %}
                        </a>
            </div>
                        {% else %}
                            <div class="d-inline-block btn-group" role="group" aria-label="Basic example">
                                <a class="btn btn-success" href="{{ url('security_login') }}">zaloguj się</a>
                                <a class="btn btn-light" href="{{ url('app_register') }}">rejestracja</a>
                            </div>
            {% endif %}
        </div>
    </div>
    <div class="row">
            <nav class="col-2 vertical-menu-contener navbar-dark bg-info text-white menu-shadow">
                <ul class="nav fz-20px vertical-menu">
                    <li><a class="nav-link text-light {% block articlesActive %}{% endblock %}" href="{{ url('article_index') }}">Artykuły</a></li>
                    <li><a class="nav-link text-light {% block categoriesActive %}{% endblock %}" href="{{ url('category_index') }}">{{ 'title.category_list'|trans }}</a></li>
                    <li><a class="last-sublist-elem nav-link text-light {% block authorsActive %}{% endblock %}" href="{{ url('author_index') }}">{{ 'title.author_list'|trans }}</a></li>
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <li><a class="nav-link text-light {% block myBlogActive %}{% endblock %}" href="{{ url('author_view', {id: app.user.id, firstName: app.user.firstName} ) }}">{{ 'title.my_blog'|trans }}</a></li>
                        <li><a class="nav-link text-light {% block subscriptionsActive %}{% endblock %}" href="{{ url('subscriptions_index') }}">{{ 'title.subscription_list'|trans }}</a></li>
                        <li><a class="nav-link text-light {% block followedActive %}{% endblock %}" href="{{ url('subscriptions_followed') }}">Obserwowani</a></li>
                        <li><a class="nav-link text-light {% block followersActive %}{% endblock %}" href="{{ url('subscriptions_followers') }}">{{ 'title.followers'|trans }}</a></li>
                        <li><a class="nav-link text-light" href="{{ url('article_new') }}" title="new">{{ 'action.add_article'|trans }}</a></li>
                        <li><a class="nav-link text-light" href="{{ url('edit_author', {id: app.user.id}) }}">Edytuj blog</a></li>
                        {% else %}
                            <li><a class="nav-link disabled" href="">{{ 'title.my_blog'|trans }}</a></li>
                            <li><a class="nav-link disabled" href="">{{ 'title.subscription_list'|trans }}</a></li>
                            <li><a class="nav-link disabled" href="">Obserwowani</a></li>
                            <li><a class="nav-link disabled" href="">{{ 'title.followers'|trans }}</a></li>
                            <li><a class="nav-link disabled" href="">{{ 'action.add_article'|trans }}</a></li>
                            <li><a class="nav-link disabled" href="">Edytuj blog</a></li>
                    {% endif %}
                </ul>
            </nav>

        <div class="content col-sm-7 offset-3 position-relative">
            {% include 'default/_flash_messages.html.twig' %}
            {% block body %}{% endblock %}
        </div>
    </div>
</div>
{% block javascripts %}
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>
{% endblock %}
</body>
</html>