{% include 'default/_form.html.twig' with {
    page_title: 'name.avatar_edit',
    attr: {
        method: 'PUT',
        action: url('avatar_edit', {'id': avatar.id}),
    },
    back_to_list_path: 'author_index',
} %}