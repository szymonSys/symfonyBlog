{% include 'default/_form.html.twig' with {
    page_title: 'title.article_edit',
    page_title_param: {'%id%': article.id},
    attr: {
        method: 'PUT',
        action: url('article_edit', {'id': article.id}),
    },
    back_to_list_path: 'article_index',
} %}